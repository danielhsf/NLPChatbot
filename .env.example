ANTHROPIC_API_KEY=your-key-here

# Vector store backend: "weaviate" (default) or "chroma".
VECTOR_STORE_BACKEND=weaviate

# Hybrid search weighting for the Weaviate backend (0 = pure BM25 keyword,
# 1 = pure vector). Ignored when VECTOR_STORE_BACKEND=chroma.
HYBRID_ALPHA=0.75
